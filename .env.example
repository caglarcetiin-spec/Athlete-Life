# These are server environment variables. Copy to .env; launch.py loads it automatically.
# Keep sqlite until you are ready to connect MongoDB.
STORAGE_BACKEND=sqlite
MONGODB_URI=
MONGODB_DATABASE=athlete_life
PORT=10000
